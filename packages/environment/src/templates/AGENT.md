# Workspace Instructions

This file defines workspace-specific operating rules.

Put repo or team constraints here: priorities, invariants, review standards, deployment rules,
or domain-specific instructions that should apply across sessions.
