export const LocalRuntimeData = () => (
  <p>
    Monad stores its sessions, configuration, credentials, skills, and memory in the daemon home directory on your device. The daemon listens on loopback by default. Data leaves the device only through model providers, tools, channels, peers, and observability endpoints that you explicitly configure or invoke.
  </p>
);
