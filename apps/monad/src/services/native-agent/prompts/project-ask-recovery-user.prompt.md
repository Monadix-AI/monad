Resume the same managed project turn with this single ordered input batch.

<%= it.batchJson %>

Each `messages` entry contains its body and delivery metadata. Process entries in `ingressSeq` order. They have already been consumed from Monad's pending inbox and must not be requested or processed again.
