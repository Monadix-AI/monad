Project Q&A summary:
Asked by: <%= it.askerName %>
Question: <%= it.question %><% if (it.options.length) { %>
Options: <%= it.options.join(' | ') %><% } %>
User answer: <%= it.answer %>

Use this as shared project context. Do not repeat it unless it changes your task-relevant response.
